# This file is intentionally left blank.
# HyDE V2 now uses Lua for configuration (hyprland.lua), but the generated
# ~/.local/share/hypr/hyprland.conf still attempts to source this file.
# Keeping this file here prevents Hyprland from crashing with a globbing error.
